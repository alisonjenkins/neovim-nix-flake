{
  snacks = {
    enable = true;

    settings = {
      animate = {
        enabled = true;
      };

      bufdelete = {
        enabled = true;
      };

      bigfile = {
        enabled = true;
      };

      scroll = {
        enabled = true;
      };

      gitbrowse = {
        enabled = true;
      };

      indent = {
        enabled = true;
      };

      notifier = {
        enabled = true;
        timeout = 3000;
      };

      picker = {
        matcher = {
          frecency = true;
          sort_empty = true;
        };
      };
    };
  };
}
