{
  trouble = {
    enable = true;

    lazyLoad.settings = {
      cmd = [ "Trouble" ];
      keys = [ "<leader>x" ];
    };

    settings = {
      auto_refresh = true;
      focus = true;
    };
  };
}
